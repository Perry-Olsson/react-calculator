import { State } from "./context/operation/types";

export const initialOperationState: State = {
  operation: [],
  previousEvent: "",
  currentNumber: "",
  validations: [],
  operationUpdates: [],
};

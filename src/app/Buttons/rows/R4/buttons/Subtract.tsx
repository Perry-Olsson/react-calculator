import React from "react";

import { Button, Text } from "../../../../../components";

export const Subtract: React.FC = () => {
  return (
    <Button>
      <Text value="-" />
    </Button>
  );
};
